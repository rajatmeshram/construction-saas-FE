[build]
  command = "npm run build"
  # Required: must differ from the site base directory or @netlify/plugin-nextjs fails.
  # The plugin still handles SSR; do not treat this as a static-only export.
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
