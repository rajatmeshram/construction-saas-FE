[build]
  command = "npm run build"
  # Leave publish unset — @netlify/plugin-nextjs sets the correct output for Next.js App Router.
  # Do NOT publish ".next" as a static folder; that causes chunk 404s.

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
